{
  "name": "aio-deck",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "src/aio-deck-frontend"
  ],
  "scripts": {
    "build": "cd src/aio-deck-frontend && npm run build",
    "deploy": "dfx deploy aio-deck-frontend"
  }
}
